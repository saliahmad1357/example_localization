import 'package:flutter/material.dart';

/// Centralized color definitions for easy updates later.
class AppColors {
  // Primary brand color (green/cream style)
  static const Color primary = Color(0xFF4CAF50);
  static const Color secondary = Color(0xFF81C784);

  // Backgrounds
  static const Color lightBackground = Color(0xFFF5F5F5);
  static const Color darkBackground = Color(0xFF121212);

  // Text colors
  static const Color textLight = Colors.black87;
  static const Color textDark = Colors.white70;

  // AppBar colors
  static const Color appBarLight = Color(0xFF4CAF50);
  static const Color appBarDark = Color(0xFF1B5E20);
}
